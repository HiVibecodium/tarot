# Test Production Environment
# For deploying without real Stripe keys

# Server
NODE_ENV=production
PORT=4000

# JWT (Generate new secrets for production!)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=YOUR_NEW_SECRET_HERE
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=YOUR_NEW_REFRESH_SECRET_HERE
JWT_REFRESH_EXPIRES_IN=7d

# CORS (update after deploy)
CORS_ORIGIN=http://localhost:4000
FRONTEND_URL=http://localhost:4000

# Features
PREMIUM_ENABLED=false
AI_ENABLED=false

# Admin
ADMIN_EMAILS=test@example.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Stripe (DISABLED for test)
# STRIPE_SECRET_KEY=
# STRIPE_PUBLISHABLE_KEY=
# STRIPE_WEBHOOK_SECRET=
# STRIPE_PREMIUM_PRICE_ID=
