# ⚡ Quick Start Guide

**AI Tarot Decision Assistant** - Быстрый старт за 5 минут

---

## 🚀 Запуск за 5 шагов

### 1️⃣ Установка зависимостей (1 минута)

```bash
# Корневые зависимости
npm install

# Frontend зависимости
cd src/frontend && npm install && cd ../..
```

### 2️⃣ Настройка окружения (30 секунд)

```bash
# .env уже существует и настроен для development
# Просто убедитесь что порты 4000 и 5173 свободны
```

### 3️⃣ Засеять карты Таро (30 секунд)

```bash
npm run db:seed
# ✅ 34 карты добавлены в базу
```

### 4️⃣ Запустить серверы (10 секунд)

```bash
npm run dev

# Автоматически запустится:
# - Backend на http://localhost:4000
# - Frontend на http://localhost:5173
```

### 5️⃣ Открыть в браузере

```
http://localhost:5173
```

---

## 🧪 Тестовый аккаунт

**Уже создан:**
```
Email: test@example.com
Password: test123
```

**Или создайте новый:**
1. Нажмите "Регистрация"
2. Введите email и пароль
3. Войдите в систему

---

## 🎯 Что попробовать

### Dashboard:
- 🔮 Вытянуть карту дня
- 🎯 Анализировать решение
- 📚 Смотреть карты Таро
- 👑 Перейти на Премиум (test mode)

### Расклад Дня:
- Один расклад в день
- Интерпретация на русском
- Сохраняется в историю

### Анализ Решения:
- Введите вопрос
- Добавьте варианты (опционально)
- Получите расклад на 3 карты
- Прошлое → Настоящее → Будущее

### Профиль:
- Просмотр статистики
- Редактирование имени
- Экспорт данных (GDPR)
- Удаление аккаунта

### Premium:
- Просмотр тарифов
- Сравнение Free vs Premium
- Тестовая оплата (нужны Stripe ключи)

---

## 🔧 Полезные команды

```bash
# Development
npm run dev                 # Запустить оба сервера
npm run server:dev          # Только backend
npm run client:dev          # Только frontend

# Database
npm run db:seed            # Засеять карты

# Testing
node scripts/test-full-flow.js     # Интеграционные тесты
npm run deploy:check               # Проверка готовности

# Production
npm run build              # Build frontend
npm run start:prod         # Production server
npm run docker:build       # Docker образ
npm run docker:run         # Запуск в Docker
```

---

## 📊 API Endpoints

**Базовый URL**: `http://localhost:4000/api`

### Auth:
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `GET /auth/me` - Текущий пользователь
- `POST /auth/logout` - Выход

### Users:
- `GET /users/profile` - Профиль
- `PUT /users/profile` - Обновить профиль
- `GET /users/stats` - Статистика
- `GET /users/export` - Экспорт данных
- `DELETE /users/account` - Удалить аккаунт

### Cards:
- `GET /cards` - Все карты
- `GET /cards/:id` - Одна карта

### Readings:
- `POST /readings/daily` - Расклад дня
- `POST /readings/decision` - Анализ решения
- `GET /readings/history` - История

### Stripe:
- `POST /stripe/create-checkout-session` - Начать оплату
- `GET /stripe/subscription-status` - Статус подписки
- `POST /stripe/cancel-subscription` - Отменить
- `POST /stripe/webhook` - Webhook

---

## 🐛 Troubleshooting

### Порт уже занят:

```bash
# Windows
netstat -ano | findstr :4000
netstat -ano | findstr :5173
taskkill /PID <PID> /F

# Или измените порты в .env
PORT=4001
```

### Карты не загружаются:

```bash
npm run db:seed
```

### Frontend не компилируется:

```bash
cd src/frontend
rm -rf node_modules
npm install
npm run dev
```

### Backend ошибки:

```bash
# Проверьте .env файл
# Проверьте что все зависимости установлены
npm install
```

---

## 📱 Страницы

| URL | Название | Доступ |
|-----|----------|--------|
| `/login` | Вход | Public |
| `/register` | Регистрация | Public |
| `/dashboard` | Главная | Auth |
| `/reading/daily` | Расклад дня | Auth |
| `/decision` | Анализ решения | Auth |
| `/profile` | Профиль | Auth |
| `/history` | История | Auth |
| `/cards` | Энциклопедия карт | Auth |
| `/premium` | Премиум | Auth |

---

## 🎴 Карты Таро

**Всего**: 34 карты
- **Старшие Арканы**: 22 (полная коллекция)
- **Младшие Арканы**: 12 (sample)

**Каждая карта имеет:**
- Русское название
- Keywords (upright/reversed)
- 3 контекста интерпретаций
- 2 ориентации
- 3 варианта каждой

---

## 💡 Tips

**Для разработки:**
- Hot reload работает автоматически
- Логи в консоли серверов
- Chrome DevTools для frontend
- Используйте Postman для API тестов

**Для демо:**
- Создайте несколько тестовых аккаунтов
- Сделайте несколько раскладов
- Покажите статистику
- Продемонстрируйте Premium страницу

---

## 📞 Поддержка

**Документация:**
- `README.md` - Обзор проекта
- `PRODUCTION-DEPLOYMENT-GUIDE.md` - Деплой
- `FINAL-MVP-REPORT.md` - Итоговый отчёт
- `CASCADE/` - TOC artifacts

**Проблемы?**
- Проверьте логи серверов
- Запустите `npm run deploy:check`
- Проверьте `.env` файл
- Запустите интеграционные тесты

---

## ✅ Готово!

**Откройте http://localhost:5173 и начинайте тестировать!** 🎉

**Время от установки до первого запуска: ~5 минут** ⚡
