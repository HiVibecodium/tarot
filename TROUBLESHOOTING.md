# 🔧 Troubleshooting Guide

**Дата**: 2025-11-07

---

## ❌ Проблема: "Login failed" / "Неверный email или пароль"

### Причина:
Пользователь с указанным email не существует в базе данных.

### Решение:

**Вариант 1: Зарегистрировать нового пользователя**
1. Кликните "Регистрация" на странице входа
2. Введите ваш email (siniyan@bk.ru)
3. Придумайте пароль (минимум 6 символов)
4. Нажмите "Зарегистрироваться"

**Вариант 2: Использовать тестовый аккаунт**
```
Email: test@example.com
Password: test123
```

**Вариант 3: Проверить список пользователей**
```bash
# Посмотреть всех пользователей в базе:
cat src/backend/db/data/users.json | grep "email"
```

---

## ❌ Проблема: Названия карт на английском

### ✅ ИСПРАВЛЕНО

Карты пересозданы с русскими названиями:
- "The Fool" → "Шут"
- "The Magician" → "Маг"
- "The High Priestess" → "Верховная Жрица"
- "Ace of Wands" → "Туз Жезлов"

**Обновите страницу** (F5) чтобы увидеть изменения.

---

## ❌ Проблема: Frontend на порту 5174 вместо 5173

### Причина:
Старый процесс node занимает порт 5173.

### Решение:
```bash
# Убить все node процессы
taskkill /F /IM node.exe

# Перезапустить серверы
npm run server:dev
cd src/frontend && npm run dev
```

**Или просто используйте**: http://localhost:5174 ✅

---

## ❌ Проблема: Сервер не запускается

### Проверка:
```bash
# Проверить что порт 4000 свободен
netstat -ano | findstr :4000

# Если занят - убить процесс
taskkill /F /PID [номер процесса]
```

### Решение:
```bash
# Перезапустить
npm run server:dev
```

---

## ❌ Проблема: Database error / Cards not found

### Решение:
```bash
# Пересоздать карты
node src/backend/scripts/seed-cards.js

# Введите "yes" когда спросит
```

---

## ✅ Быстрый Тест Системы

```bash
# 1. Backend health
curl http://localhost:4000/health

# Должно показать: {"success":true}

# 2. Создать пользователя
curl -X POST http://localhost:4000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"new@test.com","password":"test123"}'

# 3. Войти
curl -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"new@test.com","password":"test123"}'
```

---

## 🎯 Текущие Тестовые Аккаунты:

Список пользователей в базе:
```
test@example.com / test123
user2@test.com / test123
check@test.com / check123
[новые пользователи]
```

**Используйте любой или создайте новый!**

---

## 🚀 Быстрый Старт

```bash
# 1. Запустить backend
npm run server:dev

# 2. Запустить frontend (новый терминал)
cd src/frontend && npm run dev

# 3. Открыть браузер
http://localhost:5174

# 4. Создать аккаунт или войти с:
Email: test@example.com
Password: test123
```

---

**Если проблема сохраняется - создайте новый аккаунт через Регистрацию!**
