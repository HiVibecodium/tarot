# 🎴 Настройка Изображений Карт Таро

**Цель**: Добавить реальные изображения для всех 78 карт

---

## 🎯 ВАРИАНТЫ ПОЛУЧЕНИЯ ИЗОБРАЖЕНИЙ

### Вариант 1: Rider-Waite Deck (Public Domain) ⭐ Рекомендую

**Источник**: Классическая колода Rider-Waite (1909) - public domain

**Где скачать**:
1. https://commons.wikimedia.org/wiki/Category:Rider-Waite_tarot_deck
2. https://www.sacred-texts.com/tarot/pkt/index.htm
3. https://github.com/dariusk/corpora (tarot images)

**Шаги:**
```bash
# 1. Создать папку для изображений
mkdir -p src/frontend/public/cards

# 2. Скачать ZIP архив Rider-Waite колоды
# Или клонировать готовый набор:
git clone https://github.com/ekelen/tarot-cards.git temp-cards
cp temp-cards/cards/* src/frontend/public/cards/
rm -rf temp-cards

# 3. Переименовать файлы под наши ID
# major-00-fool.jpg, major-01-magician.jpg, etc.
```

---

### Вариант 2: Использовать API (Автоматически)

**Scryfall Tarot API** (бесплатный):
```javascript
// Скрипт для скачивания
const axios = require('axios');
const fs = require('fs');

// Для каждой карты
const downloadCard = async (cardId, cardName) => {
  // Поиск изображения
  const response = await axios.get(
    `https://api.scryfall.com/cards/named?fuzzy=${cardName}`
  );

  const imageUrl = response.data.image_uris.normal;
  const image = await axios.get(imageUrl, { responseType: 'arraybuffer' });

  fs.writeFileSync(
    `src/frontend/public/cards/${cardId}.jpg`,
    Buffer.from(image.data)
  );
};
```

---

### Вариант 3: Современная Минималистичная Колода

**Labyrinthos Tarot** (с лицензией для использования):
- Современный дизайн
- Минималистичный стиль
- Хорошо читается на экране

**Стоимость**: ~$20-30 за лицензию

---

## ⚡ БЫСТРОЕ РЕШЕНИЕ (5 минут)

**Используем placeholder URLs для начала:**

Обновите в `src/frontend/public/cards/`:

```bash
# Создать символические ссылки на временные изображения
# Или использовать плейсхолдеры от placeholder.com

# Для каждой карты создать:
https://via.placeholder.com/400x600/667eea/ffffff?text=The+Fool
```

**Скрипт автосоздания:**
```javascript
// scripts/generate-placeholder-images.js
const fs = require('fs');
const https = require('https');

const cards = [/* список всех 78 карт */];

cards.forEach(card => {
  const url = `https://via.placeholder.com/400x600/${card.color}/ffffff?text=${encodeURIComponent(card.name)}`;

  // Download and save
  https.get(url, (response) => {
    const file = fs.createWriteStream(`src/frontend/public/cards/${card.id}.jpg`);
    response.pipe(file);
  });
});
```

---

## 🎨 ТЕКУЩЕЕ РЕШЕНИЕ (уже есть!)

**Сейчас используем:**
- Уникальные градиенты для каждой карты ✅
- Эмодзи иконки ✅
- Символы (∞, ♈, I, II, III) ✅

**Это работает и выглядит современно!**

**Но если хочешь реальные изображения:**

---

## 📦 ГОТОВЫЙ НАБОР ИЗОБРАЖЕНИЙ

**Я могу создать скрипт который:**

1. Скачает 78 изображений Rider-Waite
2. Оптимизирует их (resize to 400x600)
3. Сохранит в правильные файлы
4. Обновит компонент для использования

**Хочешь чтобы я создал этот скрипт?**

**Или:**
- Оставить текущие градиенты (быстро, работает) ✅
- Скачать вручную и добавить позже
- Использовать платную современную колоду

**Что выбираешь?** 🎨
