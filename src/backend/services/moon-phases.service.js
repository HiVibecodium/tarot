/**
 * Moon Phases Service
 * Расчёт фаз Луны и рекомендаций
 */

class MoonPhasesService {
  /**
   * Рассчитывает фазу Луны для заданной даты
   * Использует алгоритм на основе синодического месяца (29.53 дня)
   */
  calculateMoonPhase(date = new Date()) {
    const year = date.getFullYear();
    const month = date.getMonth() + 1;
    const day = date.getDate();

    // Известное новолуние: 6 января 2000, 18:14 UTC
    const knownNewMoon = new Date(2000, 0, 6, 18, 14, 0);
    const synodicMonth = 29.530588853; // Средняя длина лунного месяца в днях

    // Вычисляем количество дней с известного новолуния
    const daysSinceKnownNewMoon = (date - knownNewMoon) / (1000 * 60 * 60 * 24);

    // Вычисляем текущую позицию в лунном цикле (0-1)
    const currentPhase = (daysSinceKnownNewMoon % synodicMonth) / synodicMonth;

    // Определяем название фазы и процент освещённости
    let phaseName, illumination, emoji, description, energy;

    if (currentPhase < 0.033 || currentPhase >= 0.967) {
      phaseName = 'Новолуние';
      emoji = '🌑';
      illumination = 0;
      description = 'Время новых начинаний и посева семян будущего';
      energy = 'Начало цикла, внутренняя работа, постановка намерений';
    } else if (currentPhase < 0.217) {
      phaseName = 'Растущий серп';
      emoji = '🌒';
      illumination = Math.round(currentPhase * 400);
      description = 'Время действий и движения вперёд';
      energy = 'Рост, развитие, активные действия';
    } else if (currentPhase < 0.283) {
      phaseName = 'Первая четверть';
      emoji = '🌓';
      illumination = 50;
      description = 'Преодоление препятствий и принятие решений';
      energy = 'Вызовы, решительность, баланс';
    } else if (currentPhase < 0.467) {
      phaseName = 'Растущая Луна';
      emoji = '🌔';
      illumination = Math.round(currentPhase * 200);
      description = 'Время максимальной активности и продуктивности';
      energy = 'Кульминация усилий, активность, экспансия';
    } else if (currentPhase < 0.533) {
      phaseName = 'Полнолуние';
      emoji = '🌕';
      illumination = 100;
      description = 'Пик энергии, завершение и озарение';
      energy = 'Максимум силы, осознание, празднование';
    } else if (currentPhase < 0.717) {
      phaseName = 'Убывающая Луна';
      emoji = '🌖';
      illumination = Math.round((1 - currentPhase) * 200);
      description = 'Время освобождения и очищения';
      energy = 'Отпускание, очищение, благодарность';
    } else if (currentPhase < 0.783) {
      phaseName = 'Последняя четверть';
      emoji = '🌗';
      illumination = 50;
      description = 'Подведение итогов и переоценка';
      energy = 'Рефлексия, завершение, мудрость';
    } else {
      phaseName = 'Убывающий серп';
      emoji = '🌘';
      illumination = Math.round((1 - currentPhase) * 400);
      description = 'Время отдыха и подготовки к новому циклу';
      energy = 'Отдых, восстановление, интроспекция';
    }

    return {
      phaseName,
      emoji,
      illumination,
      phaseValue: currentPhase,
      description,
      energy,
      date: date.toISOString(),
      recommendations: this.getRecommendations(phaseName)
    };
  }

  /**
   * Получает рекомендации для текущей фазы Луны
   */
  getRecommendations(phaseName) {
    const recommendations = {
      'Новолуние': {
        tarot: '🌟 Отличное время для раскладов на новые начинания и постановку намерений',
        general: [
          'Ставьте цели и намерения на новый лунный цикл',
          'Медитируйте и работайте с визуализацией',
          'Планируйте новые проекты',
          'Избегайте важных решений - дайте идеям созреть'
        ],
        energy: 'low',
        color: '#1a1a2e'
      },
      'Растущий серп': {
        tarot: '✨ Хорошее время для раскладов о действиях и первых шагах',
        general: [
          'Начинайте новые проекты',
          'Принимайте активные действия',
          'Учитесь новому',
          'Проявляйте инициативу'
        ],
        energy: 'medium',
        color: '#16213e'
      },
      'Первая четверть': {
        tarot: '⚡ Расклады на преодоление препятствий особенно информативны',
        general: [
          'Преодолевайте вызовы с решительностью',
          'Принимайте важные решения',
          'Корректируйте планы при необходимости',
          'Проявляйте упорство'
        ],
        energy: 'high',
        color: '#0f3460'
      },
      'Растущая Луна': {
        tarot: '🚀 Время для амбициозных раскладов и больших вопросов',
        general: [
          'Максимизируйте усилия',
          'Развивайте проекты',
          'Строите связи и партнёрства',
          'Будьте социально активны'
        ],
        energy: 'very-high',
        color: '#e94560'
      },
      'Полнолуние': {
        tarot: '🔮 ЛУЧШЕЕ время для раскладов! Максимальная энергия и ясность',
        general: [
          'Завершайте проекты',
          'Празднуйте достижения',
          'Проводите ритуалы благодарности',
          'Обращайте внимание на знаки и сны',
          'Особенно мощное время для магических практик'
        ],
        energy: 'maximum',
        color: '#f7b731'
      },
      'Убывающая Луна': {
        tarot: '🌙 Расклады на отпускание и освобождение наиболее эффективны',
        general: [
          'Отпускайте ненужное',
          'Очищайте пространство',
          'Избавляйтесь от вредных привычек',
          'Практикуйте прощение'
        ],
        energy: 'medium',
        color: '#5f27cd'
      },
      'Последняя четверть': {
        tarot: '📊 Время для раскладов на подведение итогов и уроки',
        general: [
          'Анализируйте результаты',
          'Учитесь на опыте',
          'Делитесь мудростью',
          'Готовьтесь к новому циклу'
        ],
        energy: 'low-medium',
        color: '#341f97'
      },
      'Убывающий серп': {
        tarot: '😌 Расклады на внутреннюю работу и самопознание',
        general: [
          'Отдыхайте и восстанавливайтесь',
          'Медитируйте',
          'Ведите дневник',
          'Готовьтесь к новолунию'
        ],
        energy: 'very-low',
        color: '#0c2461'
      }
    };

    return recommendations[phaseName] || recommendations['Новолуние'];
  }

  /**
   * Получает календарь фаз Луны на месяц
   */
  getMonthCalendar(year = new Date().getFullYear(), month = new Date().getMonth()) {
    const calendar = [];
    const daysInMonth = new Date(year, month + 1, 0).getDate();

    for (let day = 1; day <= daysInMonth; day++) {
      const date = new Date(year, month, day);
      const phase = this.calculateMoonPhase(date);

      calendar.push({
        date: day,
        dayOfWeek: date.toLocaleDateString('ru-RU', { weekday: 'short' }),
        phase: phase.phaseName,
        emoji: phase.emoji,
        illumination: phase.illumination,
        isSpecial: ['Новолуние', 'Полнолуние', 'Первая четверть', 'Последняя четверть'].includes(phase.phaseName)
      });
    }

    return {
      year,
      month: month + 1,
      monthName: new Date(year, month).toLocaleDateString('ru-RU', { month: 'long' }),
      days: calendar
    };
  }

  /**
   * Находит ближайшее полнолуние
   */
  getNextFullMoon(fromDate = new Date()) {
    let date = new Date(fromDate);

    // Проверяем до 35 дней вперёд (больше лунного месяца)
    for (let i = 0; i < 35; i++) {
      const phase = this.calculateMoonPhase(date);
      if (phase.phaseName === 'Полнолуние') {
        return {
          date: date.toISOString(),
          dateFormatted: date.toLocaleDateString('ru-RU', {
            day: 'numeric',
            month: 'long',
            year: 'numeric'
          }),
          daysUntil: i,
          phase
        };
      }
      date.setDate(date.getDate() + 1);
    }

    return null;
  }

  /**
   * Находит ближайшее новолуние
   */
  getNextNewMoon(fromDate = new Date()) {
    let date = new Date(fromDate);

    for (let i = 0; i < 35; i++) {
      const phase = this.calculateMoonPhase(date);
      if (phase.phaseName === 'Новолуние') {
        return {
          date: date.toISOString(),
          dateFormatted: date.toLocaleDateString('ru-RU', {
            day: 'numeric',
            month: 'long',
            year: 'numeric'
          }),
          daysUntil: i,
          phase
        };
      }
      date.setDate(date.getDate() + 1);
    }

    return null;
  }

  /**
   * Проверяет, является ли текущее время благоприятным для расклада
   */
  isFavorableForReading(date = new Date()) {
    const phase = this.calculateMoonPhase(date);
    const favorablePhases = ['Полнолуние', 'Растущая Луна', 'Первая четверть'];

    return {
      isFavorable: favorablePhases.includes(phase.phaseName),
      currentPhase: phase.phaseName,
      emoji: phase.emoji,
      reason: favorablePhases.includes(phase.phaseName)
        ? `${phase.emoji} Отличное время! ${phase.phaseName} усиливает интуицию`
        : `${phase.emoji} ${phase.phaseName} - можно делать расклад, но энергия не на пике`,
      recommendation: phase.recommendations.tarot
    };
  }
}

module.exports = new MoonPhasesService();
